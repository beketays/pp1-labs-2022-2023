#include <iostream>
#include <string>
using namespace std;
int main (){
   /* string s;
    char c;
    cin >> s;
    getline(cin, s);
    for(int i=0; i<s.size(); i++){
        if(s[i]>2){
            cout << s;
        }
    }
    cout << s; */

    string s,b="";
    getline(cin,s);
    string str[s.size()-1];
    for(int i=0;i<s.size();i++){
        string a="";
        // while(s[i]!=' '){
        //     a+=s[i];
        // if(a.size()>=3){
        //     b+=a;
        // }
        if(s[i] == ' ') {

        }
        }
cout << b;
}